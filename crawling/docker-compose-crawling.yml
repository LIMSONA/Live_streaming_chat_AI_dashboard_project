version: '3'

services:
  py-consumer:
    container_name: kafka_consumer
    build:
      context: .
      dockerfile: ./Dockerfile
    image: py-docker:0.1
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    environment:
      - KAFKA_SERVER=${KAFKA_SERVER}
    volumes:
      - ./:/usr/src/app
    command: python ./kafka_consumer.py
    depends_on:
      - py-producer

  py-producer:
    container_name: kafka_crawling
    build:
      context: .
      dockerfile: ./Dockerfile
    image: py-docker:0.1
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    environment:
      - KAFKA_SERVER=${KAFKA_SERVER}
    volumes:
      - ./:/usr/src/app
    command: python ./test_main.py

networks: 
  default: 
    external: 
      name: hy22-external-network